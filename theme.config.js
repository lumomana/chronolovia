/** @type {const} */
const themeColors = {
  primary: { light: '#C1440E', dark: '#E85C2A' },        // Terracotta
  background: { light: '#F5F1E8', dark: '#1A1A1A' },    // Crème / Gris très foncé
  surface: { light: '#FFF8F0', dark: '#2C2C2C' },       // Surface claire / Gris foncé
  foreground: { light: '#2C2C2C', dark: '#F5F1E8' },    // Gris foncé / Crème
  muted: { light: '#666666', dark: '#B0A89C' },         // Gris moyen / Gris clair
  border: { light: '#D3D3D3', dark: '#3D3D3D' },        // Gris clair / Gris
  success: { light: '#6B8E23', dark: '#9ACD32' },       // Vert archéo
  warning: { light: '#FF8C00', dark: '#FFA500' },       // Orange chaud
  error: { light: '#EF4444', dark: '#F87171' },         // Rouge
};

module.exports = { themeColors };
